# Python
__pycache__/
*.egg-info/
*.pyc

# Claude Code settings (local only)
.claude/

# Large binary artifacts — models, embeddings, checkpoints
*.pt
*.safetensors
*.npz
*.bin

# All real-world data stays local
data/

# Runtime outputs
logs/
outputs/
models/
neuralscience-main/

# Local LaTeX paper source
paper/

# Results (large; only specific outputs are tracked via git add -f)
results/

# Root-level assets (PDFs, large figures) — only docs/assets/ is tracked
/assets/

# Manim build artefacts
animations/media/
animations/__pycache__/
